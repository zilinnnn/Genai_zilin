[project]
name = "sps-genai"
version = "0.1.0"
description = "FastAPI examples for text generation and CIFAR-10 image models"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.138.0",
    "pillow>=10.0.0",
    "spacy>=3.8.14",
    "torch>=2.0.0",
    "torchvision>=0.15.0",
]
